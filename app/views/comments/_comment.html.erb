<li>
	<p><%= h(comment.body).gsub(/\n/,'<br/>').html_safe %> </p>
	<div class="comment-meta-details text-muted">
		<small> <em> Posted <%= time_ago_in_words(comment.created_at) unless comment.created_at.blank? %> ago by  </em></small> 
	</div>

<!-- Links -->
	<div class="article-actions">
		<% if logged_in? && (current_user == @article.user || current_user.admin?) %>	




			<%= link_to "Delete this article", [comment.article, comment], method: :delete, data: { confirm: "Are you sure you want to delete the article?"}, class: "btn btn-xs btn-danger" %>
		<% end %>
	</div><hr>


</li><hr>